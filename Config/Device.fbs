namespace nos.sys.device;

table DeviceInfo {
    vendor_name: string;
    model_name: string;
    topological_id: uint64;
    serial_number: string;
    flags: uint64;
}

struct RuntimeDeviceId {
    id: uint64;
}
